services:
  - type: web
    name: baby-app
    runtime: python
    buildCommand: bash build.sh
    startCommand: cd baby-app-backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: NODE_VERSION
        value: 18.17.0
