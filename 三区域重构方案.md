# 成长小助手 · 三区域重构技术方案（定稿 v2）

> 状态：规划 v3.1 已确认，技术方案已拍板，执行中
> 日期：2026-08-24

## 一、已确认决策

| 项 | 决策 |
|---|---|
| 新功能优先级 | 出行 > 日常 > 成长 |
| Tab 默认区 | 日常 |
| 出行区存储 | **现在接后端**（travel 表，跨设备同步） |
| 今日建议卡字段 | 四字段：建议奶量 + 建议间隔 + 下次喂养时间 + 睡眠提示 |
| 社区/商城 | 不做 |
| 早教/音乐归属 | 早教归成长、音乐归日常 |
| 日常区砍 | 哭声翻译器 / 家庭任务分配 / 连续打卡激励 |
| 成长区砍 | 成长相册+MV |

## 二、信息架构

三区域 Tab 切换，topbar + family-bar 常驻顶部，仅 section 区随 Tab 替换。

- **日常（teal）**：宝宝的一天 / 今日记录(喂尿睡) / 今日照护清单 / 生病模式+指南 / 喂养日历+月报 / 音乐区 / 【新】今日建议卡
- **成长（blue）**：发育概况 / 身高体重+曲线 / 成长阶段提醒 / 早教活动 / 宝宝档案编辑
- **出行（coral）**：【新】打包清单生成器 / 目的地推荐 / 出行历史记录（MVP 三件套）

## 三、前端架构

保持单文件主壳（App.jsx 当前 3231 行），不强行拆多文件（避免 state 断裂）。

### 主壳 App 保留
- 认证/家庭/宝宝：authView / family / babies / currentBabyId / currentUser
- 共享数据：feedRecords / feedEval / checklist / growthRecords / profile / stageTip / tempRecords
- UI 状态：toast / modal / editProfileModal / 各日历开关
- **新增**：`activeZone` state（默认 `'daily'`）

### 新增区域组件（同文件内函数组件，收 props）
- `DailyZone({ babyId, months, feedRecords, feedEval, checklist, ... })` — 包裹原日常区所有 section
- `GrowthZone({ babyId, months, growthRecords, profile, stageTip, ... })` — 包裹原成长区所有 section
- `TravelZone({ babyId, months })` — 出行区三件套（新建）

### Tab 切换 UI
- 移动端：底部 Tab Bar（三个按钮 + 图标，fixed bottom）
- 桌面端：顶部胶囊 Tab（family-bar 下方居中/居左）
- 切换时各区域已加载数据保留，不重新 fetch

### 出行区子状态（放 TravelZone 内部）
- 清单：listDraft / generatedList / checkItems / customItems
- 推荐：destFilter（月龄段/季节/类型）
- 历史：travelHistory[]

## 四、后端改动（出行区接后端）

在 `main.py` 的 `init_db()` 增加两张表，路由风格与现有 v2 一致（X-Baby-Id 隔离）：

### 表 1：travel_lists（打包清单生成记录）
```sql
CREATE TABLE IF NOT EXISTS travel_lists (
    id TEXT, baby_id TEXT,
    dest_type TEXT,        -- short/long/abroad
    age_months INTEGER,
    items TEXT,            -- JSON: [{cat, name, checked, custom}]
    created_at TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (baby_id, id)
);
```

### 表 2：travel_records（出行历史）
```sql
CREATE TABLE IF NOT EXISTS travel_records (
    id TEXT, baby_id TEXT,
    dest_name TEXT, dest_type TEXT,
    travel_date TEXT, age_months INTEGER,
    rating INTEGER,        -- 1-5 推荐度
    note TEXT DEFAULT '',
    created_at TEXT DEFAULT (datetime('now')),
    PRIMARY KEY (baby_id, id)
);
```

### 路由（均需 X-Baby-Id）
- `GET  /travel/lists`  — 列出该宝宝所有清单
- `POST /travel/lists`  — 生成/保存清单 `{dest_type, age_months, items}`
- `PUT  /travel/lists/{id}` — 更新勾选状态
- `DELETE /travel/lists/{id}`
- `GET  /travel/records` — 出行历史
- `POST /travel/records` — 新增历史 `{dest_name, dest_type, travel_date, age_months, rating, note}`
- `DELETE /travel/records/{id}`

> 清单「生成逻辑」前端做（月龄×目的地 → 预设清单模板），后端只存结果。
> 目的地推荐：前端内置「月龄段×季节×类型」推荐表（静态数据，无需后端）。

## 五、今日建议卡（日常区 P1，轻量版）

放在 DailyZone 顶部，数据来自已加载的 `feedEval` + `feedRecords`：
- **建议奶量**：`feedEval.effective_target`（已有字段）
- **建议间隔**：由 `feedEval.recommend_feeds` 反推（24h / recommend_feeds 小时）
- **下次喂养时间**：now + 建议间隔
- **睡眠提示**：从 `feedRecords` 过滤 `type==='sleep'` 且 `duration>0`，算最近 N 条平均间隔 + 最近一次入睡时间 → 推算下次小睡窗口

字段缺失（如 feedEval 为空）时优雅降级，不报错。

## 六、执行顺序（按 zone 分批提交）

1. **批次 1 · 骨架**：加 `activeZone` state + Tab 切换 UI（移动端底 Tab / 桌面端顶胶囊）+ 把现有 section 包进 DailyZone/GrowthZone + 新建 TravelZone 空壳。构建验证。
2. **批次 2 · 出行后端**：main.py 加两张表 + 7 个路由。构建/重启验证。
3. **批次 3 · 出行前端**：TravelZone 三件套（清单生成器 + 推荐 + 历史），串后端 API。构建验证。
4. **批次 4 · 今日建议卡**：DailyZone 顶部加建议卡（四字段）。构建验证。
5. **批次 5 · 布局重做**：单分支 + 媒体查询，桌面 1440 / 手机 375 双断点复核各区域视觉。

> 每批次本地提交，不自动 push（按约定由用户 push）。

## 七、风险对策

| 风险 | 对策 |
|---|---|
| 单文件拆组件 state 错乱 | 主壳保留全部共享 state，zone 只收 props |
| 出行区跨设备同步 | 直接后端表，不走浏览器端持久化 |
| 今日建议卡缺数据报错 | 字段缺失优雅降级 |
| 布局重做影响响应式 | 单分支 + 媒体查询，双断点复核 |
| 后端重启生效 | 改 main.py 后必须重启 uvicorn 进程 |

## 八、三区域未来候选（v3.1 优先级，暂不执行，仅记录）

- **日常 P1** 今日建议卡(本次做) / 记录人显示 · **P2** 单手极速记录 / 睡眠预测 SweetSpot · **P3** 作息模板 EASY法（已砍）/ ~~月报导出 PDF~~（2026-09-01 用户决定砍掉，月报数据已在喂养日历+月报区可查，PDF 导出非刚需）
- **成长 P1** 疫苗日历 · **P2** 里程碑打卡 / WHO百分位曲线 · **P3** 飞跃期日历 / 分领域活动库 · **P4** 发育自测 ASQ-3 / 育儿知识库
- **出行**（本次做三件套）增强：母婴室POI / 天气穿衣联动 / 装备库复用 / 出发前 N 天提醒

## 九、已确认执行细节（2026-08-24 拍板）

1. 出行清单「预设模板」维度：**目的地类型**（短途/长途/出境）× **宝宝月龄段**（0-6月/6-12月/1-2岁/2岁+）× **分类**（喂养/睡眠/如厕/健康/交通/娱乐），生成后支持勾选+自定义增删。
2. 出行历史 = **独立轻量记录**，不关联清单、不记录带了什么。字段：目的地名称 / 目的地类型 / 出行日期 / 当时宝宝月龄 / 推荐度(1-5星) / 备注。与打包清单完全脱钩。
3. 出行区接后端（travel_lists + travel_records 两张表），跨设备同步，不走浏览器端持久化。
4. Tab 默认进「日常」。
5. 今日建议卡四字段全做（建议奶量 + 建议间隔 + 下次喂养时间 + 睡眠提示）。
